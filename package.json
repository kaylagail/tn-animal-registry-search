{
  "name": "tn-animal-registry-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A static searchable mirror of the public Tennessee Animal Abuse Registry.",
  "scripts": {
    "update:data": "node scripts/update-registry.mjs",
    "serve": "npx serve public",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.96.0"
  }
}
